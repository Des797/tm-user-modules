// === SECTION: eager calls | filename: eager_calls ===
    buildRelationsPage();
    buildBlacklistPage();
    buildDataPage();
    /* Suggestions and Tags are lazy — built on first tab click, or immediately if active */